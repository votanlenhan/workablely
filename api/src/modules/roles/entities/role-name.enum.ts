export enum RoleName {
  ADMIN = 'Admin',
  MANAGER = 'Manager',
  USER = 'User',
  PHOTOGRAPHER = 'Photographer',
  // Add other specific roles from ShowRoles if they are used for general permissions
  // For example, if Art Lead has specific system permissions beyond a show context
  // ART_LEAD = 'Art Lead', 
} 